# Penguins

Exploring the Palmer Penguins dataset — data exploration, visualization,
and a classification model that predicts species from physical measurements.

## Setup

```bash
uv venv --python 3.12
source .venv/bin/activate
uv pip install pandas matplotlib seaborn scikit-learn jupyter
```

Open `penguins.ipynb` in VS Code or Jupyter.
